apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pikatwo-be-svc
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - secretName: tls-secret
  rules:
  - host: "user20.169.56.100.104.nip.io"
    http:
      paths:
      - path: /pikatwo
        pathType: Prefix
        backend:
          service:
            name: sample
            port:
              number: 5000
